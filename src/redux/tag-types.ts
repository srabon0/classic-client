export enum tagTypes {
  user = "user",
  brands = "brands",
  categories = "categories",
  products = "products",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.brands,
  tagTypes.categories,
  tagTypes.products,
]; 
